# Copy this file to `.env` and fill in real values.
# `.env` is gitignored; this example file is safe to commit.

# Base URL của RUMA backend (bao gồm prefix /api).
# Ví dụ local: http://<your-lan-ip>:8080/api
EXPO_PUBLIC_API_BASE_URL=https://ruma-ojj6.onrender.com/api
